# string-manipulation
In Java, a String is an object that represents a sequence of characters. Since strings are immutable (cannot be changed after creation), all manipulation methods return a new string instead of modifying the original one.
